@using MobileWorld.Core.Models.ViewModels
@model UserViewModel

<div style="margin-bottom:15px; border-color:grey;">
    <strong>ИД : </strong>
    [@Model.Id]
    <strong> | </strong>
    <strong>Потребителско име : </strong>
    [@Model.UserName]
    <strong> | </strong>
    <strong>Роля : </strong>
    [@String.Join(", ", Model.Role)]
    <a class="delete-border"  asp-action="Delete" asp-route-userId="@Model.Id">Изтрий</a>
    <a class="edit-border" asp-action="EditUser" asp-route-userId="@Model.Id">Редактирай</a>
</div>

